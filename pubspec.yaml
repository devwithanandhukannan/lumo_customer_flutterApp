name: customer_app
description: "LUMO Customer — Safety-First Home Services"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.3.5
  geolocator: ^13.0.4
  web_socket_channel: ^3.0.2
  firebase_core: ^4.12.1
  firebase_auth: ^6.5.6
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
